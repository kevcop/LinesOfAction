#include "player.h"
#include <iostream>
#include <string>
using namespace std;

// Player::Player() {
//	 //askPlayer();
//	 //cout << "Player " << playerName<<" has joined the game.\n";
//}

 //void Player::askPlayer() {
	// cout << "Enter name\n";
	// cin >> playerName;
 //}

 /*string Player::getName() const {
	 return playerName;
 }*/

 /*void Player::setPlayerName(const string& name) {
	 playerName = name;
 }*/
 //void Player::selectPiece(char row, int col) {
	// piece = make_pair(row, col);
 //}
 //void Player::setDestination(char row, int col) {
	// destination = make_pair(row, col);
 //}

 //pair<char, int> Player::getSelectedPiece() const {
	// return piece;
 //}

 //pair<char, int> Player::getDestination() const {
	// return destination;
 //}

 //string Player::properNotation(pair<int, int> position) const {
	// char column = static_cast<char>(position.second + 'A');
	// int rowInt = position.first + 1;
	// string rowStr = to_string(rowInt);

	// string format = "";
	// format += column;
	// format += rowStr;
	// return format;

	// //char column = static_cast<char>(position.second + 'A');
	// //int rowInt = position.first + 1;

	// //return string(1, column) + to_string(rowInt);
 //}
